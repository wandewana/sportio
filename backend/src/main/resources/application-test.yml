spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/sportio_test
    username: postgres
    password: postgres

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379

  flyway:
    url: jdbc:postgresql://localhost:5433/sportio_test
    user: postgres
    password: postgres
    clean-disabled: false
    clean-on-validation-error: true

logging:
  level:
    com.sportio: INFO
    org.springframework.test: INFO
    org.springframework.security: WARN